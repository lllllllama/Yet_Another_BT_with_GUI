# Yet-Another-BT 快速开始指南

## 🚀 5分钟上手

### 第一步：安装依赖

**Ubuntu/Debian系统：**
```bash
sudo apt update
sudo apt install build-essential libgtk-3-dev libssl-dev pkg-config
```

**CentOS/RHEL系统：**
```bash
sudo dnf install gcc gtk3-devel openssl-devel pkg-config
```

### 第二步：编译程序

**推荐：图形界面版本（GTK）**
```bash
cd yet-another-bt
bash build_gtk.sh
```

**或者：终端界面版本（ncurses）**
```bash
cd yet-another-bt
make ncurses
```

**或者：同时编译两个版本**
```bash
make all
```

### 第三步：运行下载

**GTK图形界面版本（推荐）：**
```bash
./ttorrent-gtk 你的种子文件.torrent
```

**ncurses终端版本：**
```bash
./ttorrent 你的种子文件.torrent
```

---

## 📋 两个版本对比

| 特性 | GTK版 | ncurses版 |
|------|-------|-----------|
| 界面类型 | 图形化窗口 | 终端字符界面 |
| 可执行文件 | `ttorrent-gtk` | `ttorrent` |
| 适用环境 | 桌面环境 | SSH/服务器 |
| 内存占用 | ~5MB | ~1MB |
| 用户体验 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

---

## 💡 使用示例

### 下载文件
```bash
# GTK图形界面
./ttorrent-gtk ubuntu-22.04.torrent

# ncurses终端界面
./ttorrent ubuntu-22.04.torrent
```

### 断点续传
下载中断后，只需重新运行相同命令即可自动恢复：
```bash
./ttorrent-gtk your-file.torrent
# 程序会自动检测已下载进度并继续
```

### 后台下载（ncurses版）
```bash
nohup ./ttorrent large-file.torrent > download.log 2>&1 &
tail -f download.log  # 查看日志
```

---

## 🛠️ 常见问题

### GTK版本无法启动？
```bash
# 检查是否有图形环境
echo $DISPLAY

# 如果没有图形环境，使用终端版本
./ttorrent your-file.torrent
```

### 编译错误？
```bash
# 确保已安装所有依赖
sudo apt install build-essential libgtk-3-dev libssl-dev pkg-config

# 清理后重新编译
make clean
bash build_gtk.sh
```

### 如何重新开始下载？
```bash
# 删除进度文件
rm -f *_bitmap

# 重新启动下载
./ttorrent-gtk your-file.torrent
```

---

## 📖 详细文档

- **[README.md](README.md)** - 完整项目介绍
- **[GTK_GUI_USAGE.md](GTK_GUI_USAGE.md)** - GTK图形界面详细说明
- **[USAGE.md](USAGE.md)** - 通用使用文档
- **[项目详细分析.md](项目详细分析.md)** - 技术架构分析

---

## ⚡ 快捷命令

```bash
# 编译GTK版本
bash build_gtk.sh

# 编译ncurses版本
make ncurses

# 编译两个版本
make all

# 清理编译产物
make clean

# 运行GTK版本
./ttorrent-gtk file.torrent

# 运行ncurses版本
./ttorrent file.torrent

# 停止下载
Ctrl+C
```

---

**开始你的第一次下载吧！** 🎉

如有问题，请查看完整文档或提交Issue。
